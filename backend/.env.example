# Banco de dados PostgreSQL (Render provisiona automaticamente)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT
JWT_SECRET=troque_por_um_segredo_longo_e_aleatorio
JWT_EXPIRES_IN=7d

# Admin (nutricionista)
ADMIN_EMAIL=gustavo@grnutri.com.br
ADMIN_SENHA=troque_por_senha_forte

# WhatsApp do nutricionista (apenas dígitos)
NUTRICIONISTA_WPP=5511999257883

# Origens permitidas no CORS (separadas por vírgula)
# Ex: https://gustavonutrisaas.onrender.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5500

# Porta (Render define automaticamente via PORT)
PORT=3001
